{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "matches": {
      ".indexOn": ["category"]
    },
    "tipps": {
      "$uid": {
    		".indexOn": ["category"],
      	".read": "auth != null && auth.uid == $uid"
      }
    }
  }
}
